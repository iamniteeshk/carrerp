# Secrets and personal data -- NEVER commit these.
.env
candidate.yaml
config/config.yaml
config.yaml

# Per-user career profiles (real resumes, answers, documents).
profiles/
documents/
resumes/
certificates/

# Production data root (when using sibling / env layout).
/data/
/backups/

# Runtime artifacts (legacy root layout + production names).
careerpilot.pid
/database/
/logs/
/screenshots/
# Runtime artifacts (root-level only -- not careerpilot/reports Python package).
/reports/
/profiles_browser/
/browser/
/cache/
/debug/
/health/
/temp/
/exports/
/human_interactions/

# Generated Task Scheduler wrapper (created by Register-CareerPilotStartup.ps1).
scripts/run_careerpilot_task.cmd

# Python.
__pycache__/
*.pyc
.venv/
